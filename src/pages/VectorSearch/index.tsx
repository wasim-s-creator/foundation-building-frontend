import React from 'react';
import { Typography } from '@mui/material';

const VectorSearch: React.FC = () => {
  return <Typography variant="h4">Vector Search</Typography>;
};

export default VectorSearch;
