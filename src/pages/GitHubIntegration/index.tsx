import React from 'react';
import { Typography } from '@mui/material';

const GitHubIntegration: React.FC = () => {
  return <Typography variant="h4">GitHub Integration</Typography>;
};

export default GitHubIntegration;
